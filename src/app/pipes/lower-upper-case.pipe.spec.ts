import { LowerUpperCasePipe } from './lower-upper-case.pipe';

describe('LowerUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new LowerUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
